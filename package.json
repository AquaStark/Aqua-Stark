{
  "name": "aqua-stark",
  "version": "1.0.0",
  "description": "Aqua Stark - Underwater Game Platform",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../client && npm install",
    "lint": "cd backend && npm run lint && cd ../client && npm run lint",
    "format": "cd backend && npm run format && cd ../client && npm run format"
  },
  "engines": {
    "node": ">=16"
  },
  "keywords": [
    "aqua-stark",
    "game",
    "starknet",
    "dojo",
    "blockchain"
  ],
  "author": "Aqua Stark Team",
  "license": "MIT"
}
