{
  "name": "aqua-stark-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "pnpm@10.13.1",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "dev": "pnpm --filter client dev"
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
